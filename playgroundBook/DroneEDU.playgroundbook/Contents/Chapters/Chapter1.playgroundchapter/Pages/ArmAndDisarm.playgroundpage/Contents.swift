//#-hidden-code

import UIKit
import PlaygroundSupport


//#-code-completion(everything, hide)
//#-code-completion(identifier, show, arm(), disarm(), wait(_:))

//#-end-hidden-code

/*:#localized(key: "FirstProseBlock")
 **Goal:** Learn how to arm and disarm.
 
 Arm is the first step of [drone](glossary://Drone) piloting, like 'Hello World' in coding.
 
 1. steps: Place your drone on a flat.
 2. Connect your drone taping the `Connect` button in the liveView.
 3. Type the `arm()` command.
 4. To add a pause between the commands, use the `wait()` command. Try `wait(4)` to stay 4 seconds in the arm before disarm.
 5. Are you ready?, tap **Run My Code**.
 */

//#-editable-code Tap to enter code
//#-end-editable-code



//#-hidden-code
exitProgram()
//#-end-hidden-code
